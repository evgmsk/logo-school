import { TourStepDirective } from './tour-step.directive';

describe('TourStepDirective', () => {
  it('should create an instance', () => {
    const directive = new TourStepDirective();
    expect(directive).toBeTruthy();
  });
});
